- const btnStyles = `btn_nav text-white fadeIn text-xl mx-5 px-4 py-3 rounded-lg`;

nav(class=`flex flex-wrap content-start mt-20 mx-auto`)
  button(id="home" class=`${btnStyles} ${homeActive}` type=`button`) Home
  button(id="projects" class=`${btnStyles} ${projectActive}` type=`button`) Projects
  button(id="skills" class=`${btnStyles} ${skillsActive}` type=`button`) Skills
  button(id="about" class=`${btnStyles} ${aboutActive}` type=`button`) About
  button(id="contact" class=`${btnStyles} ${contactActive}` type=`button`) Contact